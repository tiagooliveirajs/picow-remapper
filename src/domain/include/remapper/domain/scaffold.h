#ifndef REMAPPER_DOMAIN_SCAFFOLD_H
#define REMAPPER_DOMAIN_SCAFFOLD_H

/*
 * G01 compile-time marker only.
 * Product HID/device/profile semantics are introduced by later gates.
 */
enum {
    REMAPPER_DOMAIN_SCAFFOLD_VERSION = 1u,
};

#endif
